apiVersion: apps/v1
kind: Deployment

metadata:
  name: campusfit

spec:
  replicas: 2

  selector:
    matchLabels:
      app: campusfit

  template:
    metadata:
      labels:
        app: campusfit

    spec:
      containers:
      - name: campusfit
        image: amoghk22/campusfit-app:1.0